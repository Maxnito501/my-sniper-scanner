name: 4-Times Auto Sniper 🔫

on:
  schedule:
    - cron: '0 3 * * 1-5'   # 10:00 น.
    - cron: '0 10 * * 1-5'  # 17:00 น.
    - cron: '30 14 * * 1-5' # 21:30 น.
    - cron: '15 21 * * 1-5' # 04:15 น.
  workflow_dispatch:        # ปุ่มกดรันมือ

jobs:
  run-sniper:
    runs-on: ubuntu-latest
    
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install yfinance pandas requests lxml

      - name: Run Auto Bot
        env:
          # ส่งกุญแจครบ 4 ดอก (เช็คชื่อให้ตรงกับใน Settings เป๊ะๆ)
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          LINE_ACCESS_TOKEN: ${{ secrets.LINE_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
        run: python auto_bot.py
