name: 4-Times Auto Sniper 🔫 (Debug Mode)

on:
  schedule:
    - cron: '0 3 * * 1-5'   # 10.00 ไทย
    - cron: '0 10 * * 1-5'  # 17.00 ไทย
    - cron: '30 14 * * 1-5' # 21.30 ไทย
    - cron: '15 21 * * 1-5' # 04.15 ไทย
  workflow_dispatch:        # ปุ่มกดรันเอง

jobs:
  run-sniper:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'
          
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install yfinance pandas requests lxml
          
      - name: Check Files (Debug)
        run: ls -R  # สั่งให้ปริ้นรายชื่อไฟล์ออกมาดูหน่อยซิ
        
      - name: Run Auto Bot
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        # *** ตรงนี้สำคัญครับ ***
        # ถ้าไฟล์คุณโบ้ชื่อ autobot.py (ไม่มีขีด) ให้แก้บรรทัดล่างนี้เป็น autobot.py
        # แต่ถ้าแก้ชื่อไฟล์เป็น auto_bot.py แล้ว ก็ใช้บรรทัดนี้ได้เลย
        run: python auto_bot.py
        
