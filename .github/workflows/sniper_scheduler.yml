name: 4-Times Auto Sniper 🔫

on:
  schedule:
    # เวลาในนี้ต้องเป็น UTC (เวลาไทย - 7 ชั่วโมง)
    # 1. ตลาดไทยเปิด (10.00 ไทย = 03.00 UTC)
    - cron: '0 3 * * 1-5'
    # 2. ตลาดไทยปิด (17.00 ไทย = 10.00 UTC)
    - cron: '0 10 * * 1-5'
    # 3. ตลาด US เปิด (21.30 ไทย = 14.30 UTC)
    - cron: '30 14 * * 1-5'
    # 4. ตลาด US ปิด (04.15 ไทย = 21.15 UTC)
    - cron: '15 21 * * 1-5'
  
  # ให้เรากดปุ่ม Run เองได้ด้วย (เผื่ออยากเทส)
  workflow_dispatch:

jobs:
  run-sniper:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'
          
      - name: Install dependencies
        run: |
          pip install yfinance pandas requests
          
      - name: Run Auto Bot
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python auto_bot.py
