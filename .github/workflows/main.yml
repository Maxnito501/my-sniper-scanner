name: 4-Times Auto Sniper (Debug Mode 2) 🕵️

on:
  workflow_dispatch: # ปุ่มกดรันเอง

jobs:
  debug-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Show all files (Detective) 🔍
        run: |
          echo "=== รายชื่อไฟล์ทั้งหมดในระบบ ==="
          ls -R
          echo "==============================="

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'

      - name: Install Libraries
        run: pip install yfinance pandas requests

      - name: Run Bot (Try 1)
        run: python auto_bot.py
        continue-on-error: true # ถ้าพัง ให้ข้ามไปลองวิธี 2

      - name: Run Bot (Try 2 - Case Sensitive?)
        run: python Auto_Bot.py # ลองตัวใหญ่
        continue-on-error: true 

      - name: Run Bot (Try 3 - No Underscore?)
        run: python autobot.py # ลองแบบไม่มีขีด
        continue-on-error: true
